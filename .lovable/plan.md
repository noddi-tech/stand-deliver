

# Update Standup Lingo: Frequency-Agnostic Wording

**File: `src/pages/MyStandup.tsx`**

Two text changes:
1. **Line 1060** — `"Today's Focus"` → `"Focus"` (keep `"Add Focus Item"` as-is for the non-standup case)
2. **Line 1071** — `"What will you work on?"` → `"What will you focus on going forward?"`

No logic changes.

